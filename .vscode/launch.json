{
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Run Script: ng",
      "request": "launch",
      "command": "npm run ng serve",
      "cwd": "${workspaceFolder}"
    }
  ]
}
